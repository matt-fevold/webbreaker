__version__ = '2.1.26-dev1'
# TODO Before dev tag removal
# test the 9 classes that are in the Jira ticket
# specifically test Config().git that scan uses (There is a to do there)
# deprecate --upload-settings, --upload-policy, --upload-webmacros CliCK options
#   go through the affected tests (the click tests + overrides dict stuff in scan_unit_tests)
